{
  "name": "artifact-vault",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && node build-sw.mjs",
    "build:sw": "node build-sw.mjs",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/static": "^8.2.0",
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.3",
    "@types/node": "24.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^3.0.1",
    "workbox-build": "7.4.1"
  },
  "engines": {
    "node": ">=20"
  }
}
